IMG_NAME=Fomu-Dev
ENABLE_SSH=1
STAGE_LIST='stage0 stage1-fomu stage2-fomu'
FIRST_USER_NAME=fomu
FIRST_USER_PASS=fomudev

# Uncomment this if a build fails and you'd like to try again
#CONTINUE=1

# Uncomment this to have the container stick around after building.
# Useful for running:
#   docker run --rm --privileged --volumes-from=pigen_work -it pi-gen /bin/bash
# (Run 'dpkg-reconfigure qemu-user-static' as the first command)
#PRESERVE_CONTAINER=1
IMG_VERSION=v1.2.5-fomu
IMG_FILENAME=Fomu-Dev-v1.2.5-fomu
ZIP_FILENAME=Fomu-Dev-v1.2.5-fomu
